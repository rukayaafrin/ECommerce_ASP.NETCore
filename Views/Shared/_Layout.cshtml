<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
    <body>
        <div class="container">
            <main role="main" class="pb-3">
                <div style="height:75px;">
                    <div class="container">
                        <span style="float:left"><strong>Hello, @ViewData["username"]</strong></span>
                        @{
                            if (ViewData["sessionId"] == null)
                            {
                                <a class="@ViewData["Is_Login"]" asp-area="" asp-controller="Login" asp-action="Index" style="text-decoration: underline; float:right">Login</a>
                            }
                            else
                            {
                                <a class="@ViewData["Is_Login"]" asp-area="" asp-controller="Logout" asp-action="Index" style="text-decoration: underline; float:right">Logout</a>
                            }
                        }
                        
                        @{
                            if (ViewData["sessionId"] != null)
                            {
                                <span style="float:right;">&nbsp;|&nbsp;</span>
                                <a class="@ViewData["Is_MyPurchases"]" asp-area="" asp-controller="MyPurchases" asp-action="Index" style="text-decoration: underline; float:right">My Purchases</a>
                            }
                        }
                    <span style="float:right;">&nbsp;|&nbsp;</span>
                        <a class="@ViewData["Is_Home"]" asp-area="" asp-controller="Home" asp-action="Index" style="text-decoration: underline; float:right">Home</a>
                    </div>
                </div>
                @RenderBody()
            </main>
        </div>
        <footer class="border-top footer text-muted">
            <div class="container">
                &copy; 2020 - ASP.NET Core CA -
            </div>
        </footer>
        <script src="~/lib/jquery/dist/jquery.min.js"></script>
        <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
        <script src="~/js/site.js" asp-append-version="true"></script>
        @RenderSection("Scripts", required: false)
    </body>
</html>
